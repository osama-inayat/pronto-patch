RSpec.describe Pronto::Rubocop::Patched do
  it "has a version number" do
    expect(Pronto::Rubocop::Patched::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
